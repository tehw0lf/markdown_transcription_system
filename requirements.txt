# Universal Markdown Audio Transcription System Requirements
#
# NOTE: This file is kept for compatibility, but UV users should use:
#   uv sync
#
# UV will install dependencies from pyproject.toml which includes:
# - PyYAML>=6.0
# - ffmpeg-python>=0.2.0
# - openai-whisper>=20250625 (local installation, no system-wide install needed!)
#
# For legacy pip users:
PyYAML>=6.0
ffmpeg-python>=0.2.0
openai-whisper>=20250625

# System dependencies (must be installed separately):
# - ffmpeg (for audio/video processing)
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS: brew install ffmpeg
#   Arch Linux: sudo pacman -S ffmpeg