# Universal Markdown Audio Transcription System Requirements

# Configuration support
PyYAML>=6.0

# Audio/video processing dependencies
ffmpeg-python>=0.2.0

# Note: This system uses system-wide installed whisper via subprocess calls
#
# Preferred installation methods:
# 1. Package manager (recommended): sudo apt install python-openai-whisper (Ubuntu/Debian)
# 2. Global pip installation: pip install --global openai-whisper
#
# The system calls the 'whisper' command directly, not the Python module

# Optional: For advanced audio processing
# librosa>=0.10.0
# soundfile>=0.12.0